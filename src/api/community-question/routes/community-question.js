'use strict';

/**
 * community-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::community-question.community-question');
